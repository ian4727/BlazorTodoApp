<main class="main">
    <ul class="todo-list">
        @if (ItemTemplate != null)
        {
            @foreach (var pair in FilterItems(Items))
            {
                var id = pair.Key;
                var todoItem = pair.Value;
                @ItemTemplate(GetTodoItemTemplateData(todoItem))
            }
        }
    </ul>
</main>

